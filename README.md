# mysticdevenv

runs arch linux on github codespaces.

# config made to use the "privileges of the default image" in arch, for example, /dev/kvm and etc.

it runs with the user "mysticgiggle" with the password "root", but you can fork the repo (or rebuild it) to change to whatever you want.
